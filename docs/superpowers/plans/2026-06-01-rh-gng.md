# Sistema RH G&G — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir um web app em Google Apps Script que permite às 9 filiais do grupo G&G conduzir entrevistas guiadas, registrar candidatos em um Banco de Talentos com histórico imutável e disponibilizar dashboards e gestão centralizada para o RH.

**Architecture:** Apps Script V8 (HTML Service) servindo uma SPA leve (Alpine.js + Tailwind via CDN). Backend modularizado em arquivos `.gs` que operam sobre Google Sheets (banco) e Google Drive (PDFs). Sessões via `PropertiesService`, locks via `LockService`, cache via `CacheService`. Lógica de negócio isolada de `SpreadsheetApp` via camada `Repo` para permitir migração futura a Next.js + Supabase.

**Tech Stack:** Google Apps Script V8 · HTML Service · Alpine.js (CDN) · Tailwind CSS (CDN) · Chart.js (CDN) · SHA-256 nativo (Utilities) · LockService · CacheService · PropertiesService.

**Spec:** [`docs/superpowers/specs/2026-06-01-rh-gng-design.md`](../specs/2026-06-01-rh-gng-design.md)

---

## File Structure

```
/                                  ← projeto Apps Script (clasp)
├─ appsscript.json                 manifest
├─ Code.gs                         doGet, include, roteamento
├─ src/
│  ├─ Auth.gs                      login filial/admin, sessões, hash
│  ├─ Candidatos.gs                CRUD candidato + busca CPF + histórico
│  ├─ Entrevistas.gs               salvar entrevista, atualizar status
│  ├─ Config.gs                    leitura/escrita das abas _CONFIG_*
│  ├─ Relatorios.gs                agregações para dashboard admin
│  ├─ Drive.gs                     upload de PDFs (CV, laudo PCD)
│  ├─ Log.gs                       grava _LOG_HISTORICO e _LOG_ACESSOS
│  ├─ Repo.gs                      camada de acesso a Sheets (substituível)
│  ├─ Utils.gs                     uuid, validaCPF, hash, formatadores
│  ├─ Setup.gs                     setupPlanilha() (bootstrap único)
│  └─ Tests.gs                     runAllTests() + asserts
├─ ui/
│  ├─ index.html                   shell SPA (Alpine roteador)
│  ├─ login.html                   tela de login
│  ├─ painel-filial.html           dashboard da filial
│  ├─ entrevista.html              wizard 4 passos
│  ├─ banco-talentos.html          banco da filial
│  ├─ admin.html                   dashboard global admin
│  ├─ admin-config.html            CRUD de roteiro/critérios/cargos
│  ├─ styles.html                  Tailwind CDN + custom
│  └─ scripts.html                 Alpine CDN + wrapper google.script.run
└─ docs/
   ├─ CHANGELOG.md
   ├─ LGPD.md
   └─ MIGRACAO.md
```

**Cada arquivo `.gs` tem uma responsabilidade única.** UI dividida por tela (não por componente) para manter o foco. `Repo.gs` é a fronteira que isola `SpreadsheetApp` da lógica de negócio.

---

## Pré-requisitos

- Conta Google com acesso à planilha alvo (`1eZ4EXsnUV6t3w_A5K7lwGFK4zygjVGF0aQl2ykumqCo`)
- Node.js 18+ instalado (para `clasp`)
- Repositório git inicializado
- Planilha alvo aberta no navegador

---

# FASE 0 — Bootstrap do projeto

### Task 0.1: Inicializar repositório e clasp

**Files:**
- Create: `.gitignore`
- Create: `.clasp.json`
- Create: `appsscript.json`
- Create: `package.json`

- [ ] **Step 1: Inicializar git**

```bash
cd "C:/Users/juliano.correa/Desktop/G&G"
git init
git branch -M main
```

- [ ] **Step 2: Criar `.gitignore`**

```
node_modules/
.clasp.json
.clasprc.json
*.log
.DS_Store
```

- [ ] **Step 3: Instalar clasp globalmente e logar**

```bash
npm install -g @google/clasp
clasp login
```

Expected: navegador abre, login Google, "Logged in!"

- [ ] **Step 4: Criar projeto Apps Script vinculado à planilha**

```bash
clasp create --type sheets --title "RH G&G" --parentId "1eZ4EXsnUV6t3w_A5K7lwGFK4zygjVGF0aQl2ykumqCo" --rootDir .
```

Expected: cria `appsscript.json` e `.clasp.json`.

- [ ] **Step 5: Editar `appsscript.json`**

```json
{
  "timeZone": "America/Sao_Paulo",
  "dependencies": {},
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8",
  "webapp": {
    "executeAs": "USER_ACCESSING",
    "access": "ANYONE_ANONYMOUS"
  },
  "oauthScopes": [
    "https://www.googleapis.com/auth/spreadsheets.currentonly",
    "https://www.googleapis.com/auth/drive.file",
    "https://www.googleapis.com/auth/script.container.ui",
    "https://www.googleapis.com/auth/script.external_request"
  ]
}
```

- [ ] **Step 6: Criar `package.json`**

```json
{
  "name": "rh-gng",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "push": "clasp push",
    "open": "clasp open",
    "logs": "clasp logs --watch"
  }
}
```

- [ ] **Step 7: Commit inicial**

```bash
git add .gitignore appsscript.json package.json
git commit -m "chore: bootstrap apps script project"
```

---

### Task 0.2: Estrutura de pastas e arquivos vazios

**Files:**
- Create: `Code.gs`, `src/*.gs` (todos vazios com cabeçalho), `ui/*.html` (vazios)

- [ ] **Step 1: Criar `Code.gs` com stub**

```javascript
/**
 * Code.gs — entrypoint do web app.
 */
function doGet(e) {
  const tpl = HtmlService.createTemplateFromFile('ui/index');
  return tpl.evaluate()
    .setTitle('RH G&G')
    .addMetaTag('viewport', 'width=device-width, initial-scale=1')
    .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);
}

function include(filename) {
  return HtmlService.createHtmlOutputFromFile(filename).getContent();
}
```

- [ ] **Step 2: Criar arquivos `src/*.gs` com cabeçalho**

Para cada arquivo abaixo, criar com o cabeçalho `// <arquivo> — <descrição>`:
- `src/Auth.gs` — login, sessões, hash
- `src/Candidatos.gs` — CRUD candidato, busca CPF
- `src/Entrevistas.gs` — salvar entrevista, atualizar status
- `src/Config.gs` — leitura/escrita das abas _CONFIG_*
- `src/Relatorios.gs` — agregações para admin
- `src/Drive.gs` — uploads de PDF
- `src/Log.gs` — gravação dos logs
- `src/Repo.gs` — camada de acesso a Sheets
- `src/Utils.gs` — helpers
- `src/Setup.gs` — bootstrap das abas
- `src/Tests.gs` — runAllTests()

- [ ] **Step 3: Criar `ui/index.html` mínimo**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <base target="_top">
  <meta charset="utf-8">
  <?!= include('ui/styles'); ?>
</head>
<body class="bg-slate-50 text-slate-900">
  <div id="app" class="min-h-screen">
    <p class="p-8">Carregando…</p>
  </div>
  <?!= include('ui/scripts'); ?>
</body>
</html>
```

- [ ] **Step 4: Criar `ui/styles.html` com Tailwind CDN**

```html
<script src="https://cdn.tailwindcss.com"></script>
<style>
  [x-cloak] { display: none !important; }
</style>
```

- [ ] **Step 5: Criar `ui/scripts.html` com Alpine CDN + wrapper**

```html
<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
<script>
  // Promisifica google.script.run
  window.api = new Proxy({}, {
    get: (_, fn) => (...args) => new Promise((resolve, reject) => {
      google.script.run
        .withSuccessHandler(resolve)
        .withFailureHandler(reject)
        [fn](...args);
    })
  });
</script>
```

- [ ] **Step 6: Push e teste**

```bash
clasp push
clasp open
```

Expected: editor Apps Script abre; "Implantar > Implantação de teste > Web app" mostra "Carregando…" no navegador.

- [ ] **Step 7: Commit**

```bash
git add Code.gs src/ ui/
git commit -m "chore: scaffold backend modules and ui shell"
```

---

# FASE 1 — Utilitários e camada de dados

### Task 1.1: Utils básicos (uuid, hash, validaCPF) com testes

**Files:**
- Modify: `src/Utils.gs`
- Modify: `src/Tests.gs`

- [ ] **Step 1: Escrever testes em `src/Tests.gs`**

```javascript
// src/Tests.gs — runAllTests()
function _assert(cond, msg) {
  if (!cond) throw new Error('FAIL: ' + msg);
}
function _assertEq(a, b, msg) {
  if (a !== b) throw new Error('FAIL: ' + msg + ' — esperado "' + b + '", obtido "' + a + '"');
}

function runAllTests() {
  const tests = [
    test_uuid_formato,
    test_hash_determinístico,
    test_validaCPF_valido,
    test_validaCPF_invalido,
    test_validaCPF_repetidos,
  ];
  let pass = 0, fail = 0;
  tests.forEach(t => {
    try { t(); Logger.log('✓ ' + t.name); pass++; }
    catch (e) { Logger.log('✗ ' + t.name + ' — ' + e.message); fail++; }
  });
  Logger.log('Total: ' + pass + ' passou, ' + fail + ' falhou.');
  return { pass, fail };
}

function test_uuid_formato() {
  const id = uuid();
  _assert(/^[A-Z0-9-]+$/i.test(id), 'uuid deve conter alfanuméricos e hífens');
  _assert(id.length >= 8, 'uuid deve ter no mínimo 8 caracteres');
}

function test_hash_determinístico() {
  _assertEq(sha256('abc'), sha256('abc'), 'hash igual para mesma entrada');
  _assert(sha256('abc') !== sha256('abd'), 'hash diferente para entradas diferentes');
}

function test_validaCPF_valido() {
  _assert(validaCPF('529.982.247-25'), '52998224725 é válido');
  _assert(validaCPF('11144477735'), '11144477735 é válido');
}

function test_validaCPF_invalido() {
  _assert(!validaCPF('12345678900'), '12345678900 é inválido');
  _assert(!validaCPF('abc'), 'string não numérica é inválida');
  _assert(!validaCPF(''), 'vazio é inválido');
}

function test_validaCPF_repetidos() {
  _assert(!validaCPF('11111111111'), 'CPFs com todos dígitos iguais são inválidos');
}
```

- [ ] **Step 2: Rodar testes — devem falhar**

No editor Apps Script, executar `runAllTests()`. Ver logs (`Ver > Logs`).
Expected: todos falham com "ReferenceError: uuid is not defined" etc.

- [ ] **Step 3: Implementar `src/Utils.gs`**

```javascript
// src/Utils.gs — helpers puros

const SALT = 'rh-gng-v1-static-salt';

function uuid() {
  return Utilities.getUuid().toUpperCase().slice(0, 8);
}

function sha256(text) {
  const bytes = Utilities.computeDigest(
    Utilities.DigestAlgorithm.SHA_256,
    SALT + text,
    Utilities.Charset.UTF_8
  );
  return bytes.map(b => ('0' + (b & 0xff).toString(16)).slice(-2)).join('');
}

function validaCPF(cpf) {
  if (typeof cpf !== 'string') return false;
  const digits = cpf.replace(/\D/g, '');
  if (digits.length !== 11) return false;
  if (/^(\d)\1{10}$/.test(digits)) return false;
  const calc = (slice) => {
    let sum = 0;
    for (let i = 0; i < slice.length; i++) {
      sum += parseInt(slice[i], 10) * (slice.length + 1 - i);
    }
    const r = (sum * 10) % 11;
    return r === 10 ? 0 : r;
  };
  return calc(digits.slice(0, 9)) === parseInt(digits[9], 10)
      && calc(digits.slice(0, 10)) === parseInt(digits[10], 10);
}

function gerarIdEntrevista() {
  const d = new Date();
  const yyyymmdd = Utilities.formatDate(d, 'America/Sao_Paulo', 'yyyyMMdd');
  return 'ENT-' + yyyymmdd + '-' + uuid().slice(0, 4);
}

function nowISO() {
  return Utilities.formatDate(new Date(), 'America/Sao_Paulo', "yyyy-MM-dd'T'HH:mm:ss");
}
```

- [ ] **Step 4: Rodar testes — todos devem passar**

`clasp push && executar runAllTests()`
Expected: log `Total: 5 passou, 0 falhou.`

- [ ] **Step 5: Commit**

```bash
git add src/Utils.gs src/Tests.gs
git commit -m "feat: utils with uuid, sha256, validaCPF + tests"
```

---

### Task 1.2: Setup da planilha (`setupPlanilha`)

**Files:**
- Modify: `src/Setup.gs`

- [ ] **Step 1: Implementar `setupPlanilha`**

```javascript
// src/Setup.gs — bootstrap das abas

const FILIAIS = [
  { codigo: '464', nome: 'MT TREVO' },
  { codigo: '468', nome: 'MT PONTE NOVA' },
  { codigo: '743', nome: 'MS GUAICURUS' },
  { codigo: '783', nome: 'SC SAO JOSE' },
  { codigo: '264', nome: 'SC PORTO BELO' },
  { codigo: '773', nome: 'RS SAO LEOPOLDO' },
  { codigo: '713', nome: 'SP VARGEM GRANDE' },
  { codigo: '733', nome: 'SP JACAREI' },
  { codigo: '364', nome: 'DF SIA' },
];

const COLUNAS_FILIAL = [
  'id_entrevista','data_hora','filial','cpf','nome','data_nasc','rg','telefone','email','cidade',
  'cargo_pretendido','pretensao_salarial','experiencias','linkedin','escolaridade','estado_civil',
  'tem_filhos','possui_cnh','veiculo_proprio','disponibilidade_turnos','disponibilidade_inicio',
  'disponibilidade_viagem','pcd','pcd_tipo','pcd_laudo_url','indicacao','indicado_por_nome',
  'indicado_por_cargo','fumante','ja_trabalhou_grupo','ja_trabalhou_quando','curriculo_url',
  'respostas_roteiro','notas_criterios','media_notas','observacoes','nota_geral','status',
  'motivo_decisao','proxima_etapa','data_retorno','recrutador','atualizado_em','atualizado_por'
];

function nomeAbaFilial(f) {
  return f.codigo + '-' + f.nome.replace(/\s+/g, '-');
}

function setupPlanilha() {
  const ss = SpreadsheetApp.getActive();
  const senhasGeradas = [];

  // 1. Abas de filial
  FILIAIS.forEach(f => {
    const nome = nomeAbaFilial(f);
    let aba = ss.getSheetByName(nome);
    if (!aba) aba = ss.insertSheet(nome);
    aba.getRange(1, 1, 1, COLUNAS_FILIAL.length).setValues([COLUNAS_FILIAL]).setFontWeight('bold');
    aba.setFrozenRows(1);
  });

  // 2. _CONFIG_FILIAIS (gera senhas)
  _criarAba(ss, '_CONFIG_FILIAIS', ['codigo','nome','senha_hash','ativa']);
  const filiaisRows = FILIAIS.map(f => {
    const senhaPlain = f.codigo + '@' + Math.random().toString(36).slice(2, 8);
    senhasGeradas.push({ filial: f.codigo + ' - ' + f.nome, senha: senhaPlain });
    return [f.codigo, f.nome, sha256(senhaPlain), 'sim'];
  });
  ss.getSheetByName('_CONFIG_FILIAIS').getRange(2, 1, filiaisRows.length, 4).setValues(filiaisRows);

  // 3. _CONFIG_ADMIN com senha padrão
  _criarAba(ss, '_CONFIG_ADMIN', ['usuario','senha_hash']);
  const adminSenha = 'admin@' + Math.random().toString(36).slice(2, 8);
  ss.getSheetByName('_CONFIG_ADMIN').getRange(2, 1, 1, 2).setValues([['admin', sha256(adminSenha)]]);
  senhasGeradas.push({ filial: 'ADMIN (usuário: admin)', senha: adminSenha });

  // 4. _CONFIG_CARGOS
  _criarAba(ss, '_CONFIG_CARGOS', ['nome_cargo','ativo']);
  const cargosDefault = ['Vendedor','Motorista','Operador','Auxiliar Administrativo','Estoquista'];
  ss.getSheetByName('_CONFIG_CARGOS').getRange(2, 1, cargosDefault.length, 2)
    .setValues(cargosDefault.map(c => [c, 'sim']));

  // 5. _CONFIG_ROTEIRO
  _criarAba(ss, '_CONFIG_ROTEIRO', ['id','cargo','ordem','pergunta','tipo']);
  const roteiroDefault = [
    ['q1','TODOS',1,'Conte um pouco sobre você.','texto'],
    ['q2','TODOS',2,'Por que se interessa por essa vaga?','texto'],
    ['q3','TODOS',3,'Tem disponibilidade para começar imediatamente?','sim-nao'],
    ['q4','TODOS',4,'Como avalia seu nível de comunicação?','escala'],
  ];
  ss.getSheetByName('_CONFIG_ROTEIRO').getRange(2, 1, roteiroDefault.length, 5).setValues(roteiroDefault);

  // 6. _CONFIG_CRITERIOS
  _criarAba(ss, '_CONFIG_CRITERIOS', ['id','nome_criterio','escala_max','peso','ativo']);
  const criteriosDefault = [
    ['c1','Comunicação',5,1,'sim'],
    ['c2','Experiência',5,1,'sim'],
    ['c3','Apresentação',5,1,'sim'],
    ['c4','Disponibilidade',5,1,'sim'],
    ['c5','Fit cultural',5,1,'sim'],
  ];
  ss.getSheetByName('_CONFIG_CRITERIOS').getRange(2, 1, criteriosDefault.length, 5).setValues(criteriosDefault);

  // 7. _CONFIG_OPCOES
  _criarAba(ss, '_CONFIG_OPCOES', ['chave','valor']);
  const opcoes = [
    ['escolaridade','Fundamental|Médio|Técnico|Superior incompleto|Superior|Pós'],
    ['estado_civil','Solteiro|Casado|União estável|Divorciado|Viúvo'],
    ['turnos','Manhã|Tarde|Noite|Madrugada|Comercial|12x36|6x1'],
    ['cnh','Não|A|B|AB|C|D|E'],
    ['status','Aprovado|Reprovado|Banco de Talentos|Contratado|Em análise'],
  ];
  ss.getSheetByName('_CONFIG_OPCOES').getRange(2, 1, opcoes.length, 2).setValues(opcoes);

  // 8. _LOG_HISTORICO e _LOG_ACESSOS
  _criarAba(ss, '_LOG_HISTORICO', ['id_entrevista','data_hora','de_status','para_status','usuario','motivo']);
  _criarAba(ss, '_LOG_ACESSOS', ['data_hora','usuario','acao','detalhe']);

  // 9. Pasta no Drive
  const folderName = 'RH-G&G';
  const folders = DriveApp.getFoldersByName(folderName);
  const root = folders.hasNext() ? folders.next() : DriveApp.createFolder(folderName);
  const subName = 'Curriculos';
  const subs = root.getFoldersByName(subName);
  if (!subs.hasNext()) root.createFolder(subName);

  // 10. Mostra senhas no log
  Logger.log('===== SENHAS GERADAS (anote em local seguro!) =====');
  senhasGeradas.forEach(s => Logger.log(s.filial + ' → ' + s.senha));
  Logger.log('====================================================');
  return senhasGeradas;
}

function _criarAba(ss, nome, cabecalho) {
  let aba = ss.getSheetByName(nome);
  if (!aba) aba = ss.insertSheet(nome);
  else aba.clear();
  aba.getRange(1, 1, 1, cabecalho.length).setValues([cabecalho]).setFontWeight('bold');
  aba.setFrozenRows(1);
}
```

- [ ] **Step 2: Push e rodar uma vez**

```bash
clasp push
```

No editor: executar `setupPlanilha()`. Conceder permissões. Ver `Logger` para senhas.

Expected: 9 abas de filial + 7 abas `_*` + pasta `RH-G&G/Curriculos` no Drive.

- [ ] **Step 3: Conferir manualmente na planilha**

Abrir a planilha — verificar abas criadas, cabeçalhos em negrito, primeira linha congelada.

- [ ] **Step 4: Commit**

```bash
git add src/Setup.gs
git commit -m "feat: setupPlanilha bootstrap sheets and drive folder"
```

---

### Task 1.3: Camada `Repo` (acesso a Sheets isolado)

**Files:**
- Modify: `src/Repo.gs`
- Modify: `src/Tests.gs`

- [ ] **Step 1: Adicionar testes para Repo**

Append em `src/Tests.gs`:

```javascript
function test_Repo_readConfigOpcoes() {
  const op = Repo.readConfigOpcoes();
  _assert(Array.isArray(op.escolaridade), 'opções de escolaridade devem ser array');
  _assert(op.escolaridade.length > 0, 'escolaridade não vazia');
}

function test_Repo_readCargos() {
  const cargos = Repo.readCargos();
  _assert(Array.isArray(cargos), 'cargos é array');
  _assert(cargos.length > 0, 'cargos não vazio');
}

function test_Repo_filialPorCodigo() {
  const f = Repo.filialPorCodigo('464');
  _assertEq(f.nome, 'MT TREVO', 'filial 464 deve ser MT TREVO');
}
```

Adicionar essas funções à lista `tests` em `runAllTests()`.

- [ ] **Step 2: Rodar testes — devem falhar**

Expected: `Repo is not defined`

- [ ] **Step 3: Implementar `src/Repo.gs`**

```javascript
// src/Repo.gs — camada única de acesso ao Sheets

const Repo = (() => {
  const ss = () => SpreadsheetApp.getActive();

  function _readAll(sheetName) {
    const sh = ss().getSheetByName(sheetName);
    if (!sh) throw new Error('Aba não encontrada: ' + sheetName);
    const values = sh.getDataRange().getValues();
    const [head, ...rows] = values;
    return rows.filter(r => r[0] !== '').map(r => {
      const obj = {};
      head.forEach((k, i) => obj[k] = r[i]);
      return obj;
    });
  }

  function _append(sheetName, obj) {
    const sh = ss().getSheetByName(sheetName);
    const head = sh.getRange(1, 1, 1, sh.getLastColumn()).getValues()[0];
    const row = head.map(k => obj[k] === undefined ? '' : obj[k]);
    sh.appendRow(row);
  }

  function _updateById(sheetName, idCol, idVal, patch) {
    const sh = ss().getSheetByName(sheetName);
    const data = sh.getDataRange().getValues();
    const head = data[0];
    const idIdx = head.indexOf(idCol);
    for (let r = 1; r < data.length; r++) {
      if (data[r][idIdx] === idVal) {
        Object.keys(patch).forEach(k => {
          const c = head.indexOf(k);
          if (c >= 0) sh.getRange(r + 1, c + 1).setValue(patch[k]);
        });
        return true;
      }
    }
    return false;
  }

  return {
    readConfigOpcoes() {
      const rows = _readAll('_CONFIG_OPCOES');
      const out = {};
      rows.forEach(r => out[r.chave] = String(r.valor).split('|'));
      return out;
    },
    readCargos() {
      return _readAll('_CONFIG_CARGOS').filter(c => c.ativo === 'sim').map(c => c.nome_cargo);
    },
    readRoteiro(cargo) {
      const rows = _readAll('_CONFIG_ROTEIRO');
      return rows
        .filter(r => r.cargo === 'TODOS' || r.cargo === cargo)
        .sort((a, b) => a.ordem - b.ordem);
    },
    readCriterios() {
      return _readAll('_CONFIG_CRITERIOS').filter(c => c.ativo === 'sim');
    },
    filiais() {
      return _readAll('_CONFIG_FILIAIS').filter(f => f.ativa === 'sim');
    },
    filialPorCodigo(codigo) {
      return this.filiais().find(f => String(f.codigo) === String(codigo));
    },
    filialPorHash(hash) {
      return this.filiais().find(f => f.senha_hash === hash);
    },
    admin() {
      return _readAll('_CONFIG_ADMIN')[0];
    },

    appendEntrevista(filialCodigo, obj) {
      const filial = this.filialPorCodigo(filialCodigo);
      _append(nomeAbaFilial(filial), obj);
    },
    updateEntrevista(filialCodigo, idEntrevista, patch) {
      const filial = this.filialPorCodigo(filialCodigo);
      return _updateById(nomeAbaFilial(filial), 'id_entrevista', idEntrevista, patch);
    },
    findByCPFEmTodasFiliais(cpf) {
      const cpfLimpo = String(cpf).replace(/\D/g, '');
      const found = [];
      this.filiais().forEach(f => {
        const rows = _readAll(nomeAbaFilial(f));
        rows.forEach(r => {
          if (String(r.cpf).replace(/\D/g, '') === cpfLimpo) {
            found.push({ filial: f.codigo, ...r });
          }
        });
      });
      return found;
    },
    listFilial(filialCodigo) {
      const filial = this.filialPorCodigo(filialCodigo);
      return _readAll(nomeAbaFilial(filial));
    },
    listTodas() {
      const all = [];
      this.filiais().forEach(f => {
        _readAll(nomeAbaFilial(f)).forEach(r => all.push({ filial: f.codigo, ...r }));
      });
      return all;
    },

    appendHistorico(obj) { _append('_LOG_HISTORICO', obj); },
    appendAcesso(obj)    { _append('_LOG_ACESSOS', obj); },

    upsertCargo(nome, ativo) {
      const sh = ss().getSheetByName('_CONFIG_CARGOS');
      const data = sh.getDataRange().getValues();
      for (let r = 1; r < data.length; r++) {
        if (data[r][0] === nome) { sh.getRange(r + 1, 2).setValue(ativo); return; }
      }
      sh.appendRow([nome, ativo]);
    },
    upsertRoteiro(item) {
      const sh = ss().getSheetByName('_CONFIG_ROTEIRO');
      const data = sh.getDataRange().getValues();
      const head = data[0];
      for (let r = 1; r < data.length; r++) {
        if (data[r][0] === item.id) {
          head.forEach((k, c) => sh.getRange(r + 1, c + 1).setValue(item[k]));
          return;
        }
      }
      sh.appendRow(head.map(k => item[k] || ''));
    },
    deleteRoteiro(id) {
      const sh = ss().getSheetByName('_CONFIG_ROTEIRO');
      const data = sh.getDataRange().getValues();
      for (let r = 1; r < data.length; r++) {
        if (data[r][0] === id) { sh.deleteRow(r + 1); return; }
      }
    },
    upsertCriterio(item) {
      const sh = ss().getSheetByName('_CONFIG_CRITERIOS');
      const data = sh.getDataRange().getValues();
      const head = data[0];
      for (let r = 1; r < data.length; r++) {
        if (data[r][0] === item.id) {
          head.forEach((k, c) => sh.getRange(r + 1, c + 1).setValue(item[k]));
          return;
        }
      }
      sh.appendRow(head.map(k => item[k] || ''));
    },
    setFilialSenhaHash(codigo, hash) {
      const sh = ss().getSheetByName('_CONFIG_FILIAIS');
      const data = sh.getDataRange().getValues();
      for (let r = 1; r < data.length; r++) {
        if (String(data[r][0]) === String(codigo)) { sh.getRange(r + 1, 3).setValue(hash); return; }
      }
    },
  };
})();
```

- [ ] **Step 4: Rodar testes**

`clasp push && runAllTests()`
Expected: 8 testes passam (5 antigos + 3 novos).

- [ ] **Step 5: Commit**

```bash
git add src/Repo.gs src/Tests.gs
git commit -m "feat: Repo layer abstracting Sheets access"
```

---

# FASE 2 — Autenticação

### Task 2.1: Login filial e admin

**Files:**
- Modify: `src/Auth.gs`
- Modify: `src/Tests.gs`

- [ ] **Step 1: Testes**

Append em `src/Tests.gs`:

```javascript
function test_Auth_loginFilial_senhaErrada() {
  const r = Auth.login({ senha: 'senha-errada-xyz' });
  _assert(!r.ok, 'login com senha errada deve falhar');
}
function test_Auth_validarToken_invalido() {
  _assert(!Auth.validar('token-inexistente'), 'token inválido retorna false');
}
```

- [ ] **Step 2: Implementar `src/Auth.gs`**

```javascript
// src/Auth.gs

const Auth = (() => {
  const TTL_MS = 8 * 60 * 60 * 1000;

  function _saveSession(token, payload) {
    PropertiesService.getScriptProperties().setProperty('sess:' + token, JSON.stringify({
      ...payload, expira: Date.now() + TTL_MS,
    }));
  }
  function _readSession(token) {
    const raw = PropertiesService.getScriptProperties().getProperty('sess:' + token);
    if (!raw) return null;
    const obj = JSON.parse(raw);
    if (obj.expira < Date.now()) {
      PropertiesService.getScriptProperties().deleteProperty('sess:' + token);
      return null;
    }
    return obj;
  }

  return {
    login({ senha, usuario }) {
      if (!senha) return { ok: false, erro: 'Senha obrigatória' };
      const hash = sha256(senha);

      // tenta admin (usuario + senha)
      if (usuario) {
        const admin = Repo.admin();
        if (admin && admin.usuario === usuario && admin.senha_hash === hash) {
          const token = uuid() + uuid();
          _saveSession(token, { perfil: 'admin', usuario });
          Repo.appendAcesso({ data_hora: nowISO(), usuario: 'admin:' + usuario, acao: 'login', detalhe: '' });
          return { ok: true, token, perfil: 'admin', nome: 'Administrador' };
        }
        return { ok: false, erro: 'Usuário ou senha inválidos' };
      }

      // tenta filial (só senha)
      const filial = Repo.filialPorHash(hash);
      if (filial) {
        const token = uuid() + uuid();
        _saveSession(token, { perfil: 'filial', filial: filial.codigo });
        Repo.appendAcesso({ data_hora: nowISO(), usuario: 'filial:' + filial.codigo, acao: 'login', detalhe: '' });
        return { ok: true, token, perfil: 'filial', filial: filial.codigo, nome: filial.nome };
      }
      return { ok: false, erro: 'Senha inválida' };
    },
    logout(token) {
      PropertiesService.getScriptProperties().deleteProperty('sess:' + token);
      return { ok: true };
    },
    validar(token) {
      return _readSession(token);
    },
    exigir(token, perfilEsperado) {
      const s = _readSession(token);
      if (!s) throw new Error('Sessão expirada — faça login novamente.');
      if (perfilEsperado && s.perfil !== perfilEsperado) throw new Error('Acesso negado.');
      return s;
    },
    trocarSenhaFilial(token, codigo, novaSenha) {
      this.exigir(token, 'admin');
      Repo.setFilialSenhaHash(codigo, sha256(novaSenha));
      return { ok: true };
    },
  };
})();

// Endpoints públicos (chamados pelo frontend)
function authLogin(payload) { return Auth.login(payload); }
function authLogout(token) { return Auth.logout(token); }
function authMe(token) { return Auth.validar(token); }
```

- [ ] **Step 3: Push e rodar testes**

Expected: 10 testes passam.

- [ ] **Step 4: Commit**

```bash
git add src/Auth.gs src/Tests.gs
git commit -m "feat: auth with filial password and admin login + sessions"
```

---

### Task 2.2: Tela de Login

**Files:**
- Create: `ui/login.html`
- Modify: `ui/index.html`

- [ ] **Step 1: Criar `ui/login.html`**

```html
<div x-data="loginForm()" class="min-h-screen flex items-center justify-center px-4">
  <div class="w-full max-w-sm bg-white rounded-2xl shadow p-8">
    <h1 class="text-2xl font-bold mb-1">RH G&G</h1>
    <p class="text-sm text-slate-500 mb-6">Entre com a senha da sua filial.</p>

    <form @submit.prevent="entrar" class="space-y-4">
      <template x-if="modoAdmin">
        <input x-model="usuario" type="text" placeholder="Usuário admin"
               class="w-full border rounded-lg px-3 py-2" required>
      </template>
      <input x-model="senha" type="password" placeholder="Senha"
             class="w-full border rounded-lg px-3 py-2" required>

      <button type="submit"
              class="w-full bg-slate-900 text-white py-2 rounded-lg hover:bg-slate-800"
              :disabled="carregando">
        <span x-text="carregando ? 'Entrando…' : 'Entrar'"></span>
      </button>
    </form>

    <button @click="modoAdmin = !modoAdmin"
            class="mt-4 text-xs text-slate-500 hover:text-slate-900 w-full">
      <span x-text="modoAdmin ? 'Voltar ao login de filial' : 'Acesso administrativo'"></span>
    </button>

    <p x-show="erro" x-text="erro" class="mt-4 text-sm text-red-600"></p>
  </div>
</div>

<script>
function loginForm() {
  return {
    senha: '', usuario: '', modoAdmin: false, carregando: false, erro: '',
    async entrar() {
      this.carregando = true; this.erro = '';
      try {
        const r = await api.authLogin({ senha: this.senha, usuario: this.modoAdmin ? this.usuario : null });
        if (!r.ok) { this.erro = r.erro; return; }
        localStorage.setItem('rhgng:token', r.token);
        localStorage.setItem('rhgng:perfil', r.perfil);
        localStorage.setItem('rhgng:filial', r.filial || '');
        localStorage.setItem('rhgng:nome', r.nome);
        location.hash = r.perfil === 'admin' ? '#/admin' : '#/painel';
        location.reload();
      } catch (e) { this.erro = String(e); }
      finally { this.carregando = false; }
    }
  };
}
</script>
```

- [ ] **Step 2: Atualizar `ui/index.html` com roteador básico**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <base target="_top">
  <meta charset="utf-8">
  <?!= include('ui/styles'); ?>
</head>
<body class="bg-slate-50 text-slate-900">
  <div id="app" x-data="router()" x-init="init()" x-cloak class="min-h-screen">
    <template x-if="rota === 'login'"><div><?!= include('ui/login'); ?></div></template>
    <template x-if="rota === 'painel'"><div><p class="p-8">Painel filial (em breve)</p></div></template>
    <template x-if="rota === 'admin'"><div><p class="p-8">Admin (em breve)</p></div></template>
  </div>
  <?!= include('ui/scripts'); ?>
  <script>
    function router() {
      return {
        rota: 'login',
        init() {
          const token = localStorage.getItem('rhgng:token');
          const hash = location.hash.replace('#/', '') || (token ? localStorage.getItem('rhgng:perfil') : 'login');
          this.rota = token ? hash : 'login';
          window.addEventListener('hashchange', () => this.rota = location.hash.replace('#/', '') || 'login');
        }
      };
    }
  </script>
</body>
</html>
```

- [ ] **Step 3: Push e testar**

```bash
clasp push
```

Implantar nova versão. Abrir URL — tela de login aparece. Tentar senha errada → mostra erro. Tentar senha gerada no `setupPlanilha` → loga e muda rota.

- [ ] **Step 4: Commit**

```bash
git add ui/login.html ui/index.html
git commit -m "feat: login screen with filial and admin modes"
```

---

# FASE 3 — Cadastro e wizard de entrevista

### Task 3.1: Endpoints de candidato (busca por CPF)

**Files:**
- Modify: `src/Candidatos.gs`

- [ ] **Step 1: Implementar**

```javascript
// src/Candidatos.gs

function buscarPorCPF(token, cpf) {
  Auth.exigir(token);
  if (!validaCPF(cpf)) return { ok: false, erro: 'CPF inválido' };
  const cache = CacheService.getScriptCache();
  const key = 'cpf:' + cpf.replace(/\D/g, '');
  const cached = cache.get(key);
  if (cached) return JSON.parse(cached);

  const rows = Repo.findByCPFEmTodasFiliais(cpf);
  const dadosUltimo = rows.length ? rows[rows.length - 1] : null;
  const resp = {
    ok: true,
    encontrado: rows.length > 0,
    historico: rows.map(r => ({
      id: r.id_entrevista, data: r.data_hora, filial: r.filial,
      cargo: r.cargo_pretendido, status: r.status, nota: r.nota_geral
    })),
    dados: dadosUltimo,
  };
  cache.put(key, JSON.stringify(resp), 300);
  return resp;
}
```

- [ ] **Step 2: Push + smoke test no editor**

Executar manualmente `buscarPorCPF('token-fake', '52998224725')` — espera erro de sessão. Login real via UI, copiar token do localStorage, chamar com ele → resposta com `encontrado: false`.

- [ ] **Step 3: Commit**

```bash
git add src/Candidatos.gs
git commit -m "feat: buscarPorCPF endpoint with cross-filial history"
```

---

### Task 3.2: Endpoint `bootstrap` (carrega configs para o frontend)

**Files:**
- Modify: `src/Config.gs`

- [ ] **Step 1: Implementar**

```javascript
// src/Config.gs

function bootstrap(token) {
  const sess = Auth.exigir(token);
  return {
    perfil: sess.perfil,
    filial: sess.filial || null,
    cargos: Repo.readCargos(),
    opcoes: Repo.readConfigOpcoes(),
    criterios: Repo.readCriterios(),
    filiais: sess.perfil === 'admin' ? Repo.filiais() : [],
  };
}

function getRoteiro(token, cargo) {
  Auth.exigir(token);
  return Repo.readRoteiro(cargo || 'TODOS');
}
```

- [ ] **Step 2: Commit**

```bash
git add src/Config.gs
git commit -m "feat: bootstrap and getRoteiro endpoints"
```

---

### Task 3.3: Endpoint `salvarEntrevista`

**Files:**
- Modify: `src/Entrevistas.gs`

- [ ] **Step 1: Implementar com LockService**

```javascript
// src/Entrevistas.gs

function salvarEntrevista(token, payload) {
  const sess = Auth.exigir(token, 'filial');
  if (!validaCPF(payload.cpf)) return { ok: false, erro: 'CPF inválido' };
  if (!payload.nome)            return { ok: false, erro: 'Nome obrigatório' };
  if (!payload.cargo_pretendido) return { ok: false, erro: 'Cargo obrigatório' };
  if (!payload.status)          return { ok: false, erro: 'Status obrigatório' };
  if (payload.status === 'Reprovado' && !payload.motivo_decisao) {
    return { ok: false, erro: 'Motivo obrigatório para reprovação' };
  }

  const lock = LockService.getScriptLock();
  lock.waitLock(15000);
  try {
    const id = gerarIdEntrevista();
    const agora = nowISO();
    const media = _calcularMedia(payload.notas_criterios);

    const row = {
      id_entrevista: id,
      data_hora: agora,
      filial: sess.filial,
      cpf: String(payload.cpf).replace(/\D/g, ''),
      nome: payload.nome,
      data_nasc: payload.data_nasc || '',
      rg: payload.rg || '',
      telefone: payload.telefone || '',
      email: payload.email || '',
      cidade: payload.cidade || '',
      cargo_pretendido: payload.cargo_pretendido,
      pretensao_salarial: payload.pretensao_salarial || '',
      experiencias: payload.experiencias || '',
      linkedin: payload.linkedin || '',
      escolaridade: payload.escolaridade || '',
      estado_civil: payload.estado_civil || '',
      tem_filhos: payload.tem_filhos || '',
      possui_cnh: payload.possui_cnh || '',
      veiculo_proprio: payload.veiculo_proprio || '',
      disponibilidade_turnos: (payload.disponibilidade_turnos || []).join('|'),
      disponibilidade_inicio: payload.disponibilidade_inicio || '',
      disponibilidade_viagem: payload.disponibilidade_viagem || '',
      pcd: payload.pcd || '',
      pcd_tipo: payload.pcd_tipo || '',
      pcd_laudo_url: payload.pcd_laudo_url || '',
      indicacao: payload.indicacao || '',
      indicado_por_nome: payload.indicado_por_nome || '',
      indicado_por_cargo: payload.indicado_por_cargo || '',
      fumante: payload.fumante || '',
      ja_trabalhou_grupo: payload.ja_trabalhou_grupo || '',
      ja_trabalhou_quando: payload.ja_trabalhou_quando || '',
      curriculo_url: payload.curriculo_url || '',
      respostas_roteiro: JSON.stringify(payload.respostas_roteiro || {}),
      notas_criterios: JSON.stringify(payload.notas_criterios || {}),
      media_notas: media,
      observacoes: payload.observacoes || '',
      nota_geral: payload.nota_geral || '',
      status: payload.status,
      motivo_decisao: payload.motivo_decisao || '',
      proxima_etapa: payload.proxima_etapa || '',
      data_retorno: payload.data_retorno || '',
      recrutador: 'filial:' + sess.filial,
      atualizado_em: agora,
      atualizado_por: 'filial:' + sess.filial,
    };

    Repo.appendEntrevista(sess.filial, row);
    Repo.appendHistorico({
      id_entrevista: id, data_hora: agora,
      de_status: '', para_status: payload.status,
      usuario: 'filial:' + sess.filial,
      motivo: payload.motivo_decisao || '',
    });

    CacheService.getScriptCache().remove('cpf:' + row.cpf);
    return { ok: true, id };
  } finally {
    lock.releaseLock();
  }
}

function atualizarStatus(token, payload) {
  const sess = Auth.exigir(token);
  if (!payload.id || !payload.para_status) return { ok: false, erro: 'Dados incompletos' };
  if (payload.para_status === 'Reprovado' && !payload.motivo) {
    return { ok: false, erro: 'Motivo obrigatório' };
  }

  const lock = LockService.getScriptLock();
  lock.waitLock(15000);
  try {
    const filialCodigo = sess.perfil === 'admin' ? payload.filial : sess.filial;
    if (!filialCodigo) return { ok: false, erro: 'Filial não informada' };

    const linhas = Repo.listFilial(filialCodigo).filter(r => r.id_entrevista === payload.id);
    if (!linhas.length) return { ok: false, erro: 'Entrevista não encontrada' };
    const atual = linhas[0];

    const usuario = sess.perfil === 'admin' ? 'admin:' + sess.usuario : 'filial:' + sess.filial;
    Repo.updateEntrevista(filialCodigo, payload.id, {
      status: payload.para_status,
      motivo_decisao: payload.motivo || atual.motivo_decisao,
      proxima_etapa: payload.proxima_etapa || atual.proxima_etapa,
      data_retorno: payload.data_retorno || atual.data_retorno,
      atualizado_em: nowISO(),
      atualizado_por: usuario,
    });
    Repo.appendHistorico({
      id_entrevista: payload.id, data_hora: nowISO(),
      de_status: atual.status, para_status: payload.para_status,
      usuario, motivo: payload.motivo || '',
    });
    return { ok: true };
  } finally {
    lock.releaseLock();
  }
}

function _calcularMedia(notas) {
  if (!notas) return '';
  const vals = Object.values(notas).filter(v => typeof v === 'number');
  if (!vals.length) return '';
  return Math.round((vals.reduce((a, b) => a + b, 0) / vals.length) * 100) / 100;
}
```

- [ ] **Step 2: Push e smoke test pelo console**

Apps Script Editor → Executar `runAllTests()` (deve continuar passando 10 testes) e em seguida montar manualmente `salvarEntrevista` com token de filial real e payload mínimo.

- [ ] **Step 3: Commit**

```bash
git add src/Entrevistas.gs
git commit -m "feat: salvarEntrevista and atualizarStatus with locking"
```

---

### Task 3.4: Upload de PDF (currículo + laudo PCD)

**Files:**
- Modify: `src/Drive.gs`

- [ ] **Step 1: Implementar**

```javascript
// src/Drive.gs

function uploadPDF(token, payload) {
  const sess = Auth.exigir(token);
  if (!payload.base64 || !payload.nomeArquivo) return { ok: false, erro: 'Dados ausentes' };

  const filialCodigo = sess.filial || payload.filial || 'ADMIN';
  const pasta = _garantirPastaFilial(filialCodigo);
  const blob = Utilities.newBlob(
    Utilities.base64Decode(payload.base64),
    'application/pdf',
    payload.nomeArquivo
  );
  const file = pasta.createFile(blob);
  file.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW);
  return { ok: true, url: file.getUrl() };
}

function _garantirPastaFilial(codigo) {
  const root = _pasta('RH-G&G');
  const cur = _pasta('Curriculos', root);
  return _pasta(codigo, cur);
}
function _pasta(nome, parent) {
  const base = parent || DriveApp.getRootFolder();
  const it = base.getFoldersByName(nome);
  return it.hasNext() ? it.next() : base.createFolder(nome);
}
```

- [ ] **Step 2: Commit**

```bash
git add src/Drive.gs
git commit -m "feat: upload PDF to drive per filial"
```

---

### Task 3.5: UI — Painel da Filial

**Files:**
- Create: `ui/painel-filial.html`
- Modify: `ui/index.html`

- [ ] **Step 1: Implementar `ui/painel-filial.html`**

```html
<div x-data="painelFilial()" x-init="carregar()" class="min-h-screen bg-slate-50">
  <header class="bg-white border-b px-6 py-4 flex justify-between items-center">
    <div>
      <h1 class="text-xl font-bold">Filial <span x-text="ctx.filial"></span> — <span x-text="ctx.nome"></span></h1>
      <p class="text-xs text-slate-500">Painel de entrevistas</p>
    </div>
    <button @click="sair" class="text-sm text-slate-500 hover:text-slate-900">Sair</button>
  </header>

  <main class="p-6 max-w-6xl mx-auto">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
      <template x-for="c in cards">
        <div class="bg-white rounded-xl p-4 shadow-sm">
          <p class="text-xs uppercase text-slate-500" x-text="c.label"></p>
          <p class="text-2xl font-bold" x-text="c.valor"></p>
        </div>
      </template>
    </div>

    <div class="flex gap-3 mb-4">
      <a href="#/entrevista" class="bg-slate-900 text-white px-4 py-2 rounded-lg hover:bg-slate-800">+ Nova entrevista</a>
      <a href="#/banco" class="bg-white border px-4 py-2 rounded-lg hover:bg-slate-100">📚 Banco de Talentos</a>
    </div>

    <input x-model="busca" placeholder="Buscar por nome ou CPF..." class="border rounded-lg px-3 py-2 w-full mb-3">

    <div class="bg-white rounded-xl shadow-sm overflow-x-auto">
      <table class="min-w-full text-sm">
        <thead class="bg-slate-100"><tr>
          <th class="px-3 py-2 text-left">Data</th>
          <th class="px-3 py-2 text-left">Candidato</th>
          <th class="px-3 py-2 text-left">CPF</th>
          <th class="px-3 py-2 text-left">Cargo</th>
          <th class="px-3 py-2 text-left">Nota</th>
          <th class="px-3 py-2 text-left">Status</th>
        </tr></thead>
        <tbody>
          <template x-for="r in filtradas" :key="r.id_entrevista">
            <tr class="border-t hover:bg-slate-50">
              <td class="px-3 py-2" x-text="r.data_hora?.slice(0,10)"></td>
              <td class="px-3 py-2" x-text="r.nome"></td>
              <td class="px-3 py-2" x-text="r.cpf"></td>
              <td class="px-3 py-2" x-text="r.cargo_pretendido"></td>
              <td class="px-3 py-2" x-text="r.nota_geral"></td>
              <td class="px-3 py-2" x-text="r.status"></td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
  </main>
</div>

<script>
function painelFilial() {
  return {
    ctx: { filial: localStorage.getItem('rhgng:filial'), nome: localStorage.getItem('rhgng:nome') },
    cards: [], registros: [], busca: '',
    get filtradas() {
      const q = this.busca.toLowerCase();
      return this.registros.filter(r =>
        !q || (r.nome || '').toLowerCase().includes(q) || (String(r.cpf) || '').includes(q));
    },
    async carregar() {
      const token = localStorage.getItem('rhgng:token');
      const all = await api.listarFilial(token);
      this.registros = (all || []).reverse();
      const hoje = new Date().toISOString().slice(0, 10);
      this.cards = [
        { label: 'Entrevistas hoje', valor: this.registros.filter(r => (r.data_hora || '').startsWith(hoje)).length },
        { label: 'Banco de Talentos', valor: this.registros.filter(r => r.status === 'Banco de Talentos').length },
        { label: 'Aguardando retorno', valor: this.registros.filter(r => r.data_retorno && r.status === 'Em análise').length },
        { label: 'Aprovados no mês', valor: this.registros.filter(r => r.status === 'Aprovado' && (r.data_hora || '').slice(0,7) === hoje.slice(0,7)).length },
      ];
    },
    sair() {
      const t = localStorage.getItem('rhgng:token');
      api.authLogout(t).finally(() => {
        localStorage.clear(); location.hash = '#/login'; location.reload();
      });
    }
  };
}
</script>
```

- [ ] **Step 2: Adicionar endpoint `listarFilial` em `src/Candidatos.gs`**

Append:

```javascript
function listarFilial(token) {
  const sess = Auth.exigir(token, 'filial');
  return Repo.listFilial(sess.filial);
}
```

- [ ] **Step 3: Atualizar `ui/index.html` para incluir o painel**

Substituir o template do `painel` no `index.html`:

```html
<template x-if="rota === 'painel'"><div><?!= include('ui/painel-filial'); ?></div></template>
```

- [ ] **Step 4: Push e testar**

Login filial → painel mostra cards zerados e tabela vazia.

- [ ] **Step 5: Commit**

```bash
git add ui/painel-filial.html ui/index.html src/Candidatos.gs
git commit -m "feat: filial dashboard screen"
```

---

### Task 3.6: UI — Wizard de Entrevista (4 passos)

**Files:**
- Create: `ui/entrevista.html`
- Modify: `ui/index.html`

- [ ] **Step 1: Criar `ui/entrevista.html` com os 4 passos**

```html
<div x-data="wizard()" x-init="iniciar()" class="min-h-screen bg-slate-50">
  <header class="bg-white border-b px-6 py-4 flex justify-between">
    <h1 class="text-lg font-bold">Nova Entrevista — Passo <span x-text="passo"></span>/4</h1>
    <a href="#/painel" class="text-sm text-slate-500 hover:text-slate-900">← Voltar</a>
  </header>

  <main class="max-w-3xl mx-auto p-6 space-y-6">
    <!-- Passo 1: Identificação -->
    <section x-show="passo === 1" class="bg-white rounded-xl p-6 shadow-sm space-y-4">
      <h2 class="text-lg font-semibold">1. Identificação</h2>
      <div>
        <label class="block text-sm">CPF *</label>
        <input x-model="dados.cpf" @blur="buscarCPF" placeholder="000.000.000-00"
               class="border rounded-lg px-3 py-2 w-full">
        <template x-if="historico.length">
          <div class="mt-2 bg-yellow-50 border border-yellow-200 rounded p-3 text-sm">
            <p class="font-medium">⚠ Candidato já entrevistado antes:</p>
            <ul class="list-disc ml-5">
              <template x-for="h in historico">
                <li><span x-text="h.data?.slice(0,10)"></span> — filial <span x-text="h.filial"></span> — <span x-text="h.cargo"></span> — status <span x-text="h.status"></span></li>
              </template>
            </ul>
            <button @click="usarDadosAnteriores" class="mt-2 text-xs underline">Usar dados anteriores</button>
          </div>
        </template>
      </div>
      <div class="grid grid-cols-2 gap-4">
        <div><label class="text-sm">Nome *</label><input x-model="dados.nome" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Data nasc.</label><input type="date" x-model="dados.data_nasc" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">RG</label><input x-model="dados.rg" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Telefone</label><input x-model="dados.telefone" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Email</label><input type="email" x-model="dados.email" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Cidade</label><input x-model="dados.cidade" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Cargo pretendido *</label>
          <select x-model="dados.cargo_pretendido" @change="carregarRoteiro" class="border rounded-lg px-3 py-2 w-full">
            <option value="">—</option>
            <template x-for="c in ctx.cargos"><option :value="c" x-text="c"></option></template>
          </select>
        </div>
        <div><label class="text-sm">Escolaridade</label>
          <select x-model="dados.escolaridade" class="border rounded-lg px-3 py-2 w-full">
            <option value="">—</option>
            <template x-for="o in ctx.opcoes.escolaridade"><option :value="o" x-text="o"></option></template>
          </select>
        </div>
        <div><label class="text-sm">Estado civil</label>
          <select x-model="dados.estado_civil" class="border rounded-lg px-3 py-2 w-full">
            <option value="">—</option>
            <template x-for="o in ctx.opcoes.estado_civil"><option :value="o" x-text="o"></option></template>
          </select>
        </div>
        <div><label class="text-sm">Tem filhos?</label>
          <select x-model="dados.tem_filhos" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
      </div>
    </section>

    <!-- Passo 2: Perfil e disponibilidade -->
    <section x-show="passo === 2" class="bg-white rounded-xl p-6 shadow-sm space-y-4">
      <h2 class="text-lg font-semibold">2. Perfil &amp; Disponibilidade</h2>
      <div class="grid grid-cols-2 gap-4">
        <div class="col-span-2"><label class="text-sm">Experiências anteriores</label><textarea x-model="dados.experiencias" rows="3" class="border rounded-lg px-3 py-2 w-full"></textarea></div>
        <div><label class="text-sm">Pretensão salarial</label><input type="number" x-model="dados.pretensao_salarial" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">LinkedIn</label><input x-model="dados.linkedin" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">CNH</label>
          <select x-model="dados.possui_cnh" class="border rounded-lg px-3 py-2 w-full">
            <option value="">—</option>
            <template x-for="o in ctx.opcoes.cnh"><option :value="o" x-text="o"></option></template>
          </select>
        </div>
        <div><label class="text-sm">Veículo próprio?</label>
          <select x-model="dados.veiculo_proprio" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
        <div class="col-span-2">
          <label class="text-sm">Disponibilidade de turnos</label>
          <div class="flex flex-wrap gap-2 mt-1">
            <template x-for="t in ctx.opcoes.turnos">
              <label class="text-sm flex items-center gap-1 border rounded px-2 py-1">
                <input type="checkbox" :value="t" x-model="dados.disponibilidade_turnos">
                <span x-text="t"></span>
              </label>
            </template>
          </div>
        </div>
        <div><label class="text-sm">Pode iniciar em</label><input type="date" x-model="dados.disponibilidade_inicio" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Disponível para viagem?</label>
          <select x-model="dados.disponibilidade_viagem" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
        <div><label class="text-sm">PCD?</label>
          <select x-model="dados.pcd" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
        <div x-show="dados.pcd === 'sim'"><label class="text-sm">Tipo PCD</label><input x-model="dados.pcd_tipo" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Indicação?</label>
          <select x-model="dados.indicacao" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
        <div x-show="dados.indicacao === 'sim'"><label class="text-sm">Indicado por (nome)</label><input x-model="dados.indicado_por_nome" class="border rounded-lg px-3 py-2 w-full"></div>
        <div x-show="dados.indicacao === 'sim'"><label class="text-sm">Cargo do indicador</label><input x-model="dados.indicado_por_cargo" class="border rounded-lg px-3 py-2 w-full"></div>
        <div><label class="text-sm">Fumante?</label>
          <select x-model="dados.fumante" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
        <div><label class="text-sm">Já trabalhou no grupo?</label>
          <select x-model="dados.ja_trabalhou_grupo" class="border rounded-lg px-3 py-2 w-full"><option value="">—</option><option>sim</option><option>não</option></select>
        </div>
        <div x-show="dados.ja_trabalhou_grupo === 'sim'" class="col-span-2"><label class="text-sm">Quando/onde?</label><input x-model="dados.ja_trabalhou_quando" class="border rounded-lg px-3 py-2 w-full"></div>
        <div class="col-span-2">
          <label class="text-sm">Currículo (PDF)</label>
          <input type="file" accept="application/pdf" @change="uploadCV($event)" class="text-sm">
          <span x-show="dados.curriculo_url" class="text-xs text-green-700 ml-2">✓ enviado</span>
        </div>
      </div>
    </section>

    <!-- Passo 3: Roteiro -->
    <section x-show="passo === 3" class="bg-white rounded-xl p-6 shadow-sm space-y-4">
      <h2 class="text-lg font-semibold">3. Roteiro de perguntas</h2>
      <template x-for="p in roteiro" :key="p.id">
        <div>
          <label class="text-sm font-medium" x-text="p.ordem + '. ' + p.pergunta"></label>
          <template x-if="p.tipo === 'texto'">
            <textarea x-model="dados.respostas_roteiro[p.id]" rows="2" class="border rounded-lg px-3 py-2 w-full"></textarea>
          </template>
          <template x-if="p.tipo === 'sim-nao'">
            <select x-model="dados.respostas_roteiro[p.id]" class="border rounded-lg px-3 py-2 w-full">
              <option value="">—</option><option>sim</option><option>não</option>
            </select>
          </template>
          <template x-if="p.tipo === 'escala'">
            <input type="number" min="1" max="5" x-model.number="dados.respostas_roteiro[p.id]" class="border rounded-lg px-3 py-2 w-32">
          </template>
        </div>
      </template>
    </section>

    <!-- Passo 4: Avaliação -->
    <section x-show="passo === 4" class="bg-white rounded-xl p-6 shadow-sm space-y-4">
      <h2 class="text-lg font-semibold">4. Avaliação &amp; Decisão</h2>
      <template x-for="c in ctx.criterios" :key="c.id">
        <div class="flex items-center gap-3">
          <span class="w-48 text-sm" x-text="c.nome_criterio"></span>
          <input type="range" :min="1" :max="c.escala_max" x-model.number="dados.notas_criterios[c.id]" class="flex-1">
          <span class="w-8 text-right text-sm" x-text="dados.notas_criterios[c.id] || 0"></span>
        </div>
      </template>
      <p class="text-sm">Média: <strong x-text="media"></strong></p>
      <div><label class="text-sm">Observações</label><textarea x-model="dados.observacoes" rows="3" class="border rounded-lg px-3 py-2 w-full"></textarea></div>
      <div><label class="text-sm">Nota geral (0–10)</label><input type="number" min="0" max="10" x-model.number="dados.nota_geral" class="border rounded-lg px-3 py-2 w-32"></div>
      <div><label class="text-sm">Decisão *</label>
        <div class="flex gap-2 mt-1">
          <button @click="dados.status='Aprovado'" :class="dados.status==='Aprovado'?'bg-green-600 text-white':'bg-white border'" class="px-3 py-2 rounded-lg">🟢 Aprovado</button>
          <button @click="dados.status='Banco de Talentos'" :class="dados.status==='Banco de Talentos'?'bg-yellow-500 text-white':'bg-white border'" class="px-3 py-2 rounded-lg">🟡 Banco de Talentos</button>
          <button @click="dados.status='Reprovado'" :class="dados.status==='Reprovado'?'bg-red-600 text-white':'bg-white border'" class="px-3 py-2 rounded-lg">🔴 Reprovado</button>
        </div>
      </div>
      <div x-show="dados.status === 'Reprovado'"><label class="text-sm">Motivo da decisão *</label><textarea x-model="dados.motivo_decisao" rows="2" class="border rounded-lg px-3 py-2 w-full"></textarea></div>
      <div><label class="text-sm">Próxima etapa</label><input x-model="dados.proxima_etapa" class="border rounded-lg px-3 py-2 w-full"></div>
      <div><label class="text-sm">Data de retorno</label><input type="date" x-model="dados.data_retorno" class="border rounded-lg px-3 py-2 w-full"></div>
    </section>

    <div class="flex justify-between">
      <button @click="passo--" x-show="passo > 1" class="px-4 py-2 rounded-lg border">← Anterior</button>
      <button @click="proximo" x-show="passo < 4" class="ml-auto bg-slate-900 text-white px-4 py-2 rounded-lg">Próximo →</button>
      <button @click="salvar" x-show="passo === 4" class="ml-auto bg-green-600 text-white px-4 py-2 rounded-lg" :disabled="salvando">
        <span x-text="salvando ? 'Salvando…' : 'Salvar entrevista'"></span>
      </button>
    </div>
    <p x-show="erro" x-text="erro" class="text-red-600 text-sm"></p>
  </main>
</div>

<script>
function wizard() {
  return {
    passo: 1, salvando: false, erro: '', historico: [], roteiro: [],
    ctx: { cargos: [], opcoes: {}, criterios: [] },
    dados: {
      cpf:'', nome:'', cargo_pretendido:'', status:'',
      respostas_roteiro:{}, notas_criterios:{}, disponibilidade_turnos:[],
    },
    get media() {
      const v = Object.values(this.dados.notas_criterios).filter(n => typeof n === 'number');
      if (!v.length) return 0;
      return Math.round((v.reduce((a,b)=>a+b,0)/v.length)*100)/100;
    },
    async iniciar() {
      const token = localStorage.getItem('rhgng:token');
      this.ctx = await api.bootstrap(token);
      this._restaurarRascunho();
      setInterval(() => this._salvarRascunho(), 30000);
    },
    _salvarRascunho() {
      localStorage.setItem('rhgng:rascunho', JSON.stringify({ dados: this.dados, passo: this.passo }));
    },
    _restaurarRascunho() {
      const r = localStorage.getItem('rhgng:rascunho');
      if (r && confirm('Recuperar rascunho de entrevista não salva?')) {
        const obj = JSON.parse(r);
        Object.assign(this.dados, obj.dados);
        this.passo = obj.passo;
      }
    },
    async buscarCPF() {
      if (!this.dados.cpf) return;
      const token = localStorage.getItem('rhgng:token');
      const r = await api.buscarPorCPF(token, this.dados.cpf);
      if (r.ok) this.historico = r.historico;
    },
    usarDadosAnteriores() {
      // Re-busca para obter dados completos
      const token = localStorage.getItem('rhgng:token');
      api.buscarPorCPF(token, this.dados.cpf).then(r => {
        if (r.dados) Object.assign(this.dados, {
          nome: r.dados.nome, data_nasc: r.dados.data_nasc, rg: r.dados.rg,
          telefone: r.dados.telefone, email: r.dados.email, cidade: r.dados.cidade,
        });
      });
    },
    async carregarRoteiro() {
      const token = localStorage.getItem('rhgng:token');
      this.roteiro = await api.getRoteiro(token, this.dados.cargo_pretendido);
    },
    async uploadCV(ev) {
      const file = ev.target.files[0];
      if (!file) return;
      const b64 = await this._toBase64(file);
      const token = localStorage.getItem('rhgng:token');
      const r = await api.uploadPDF(token, { base64: b64, nomeArquivo: this.dados.cpf + '_' + (this.dados.nome || 'cv') + '.pdf' });
      if (r.ok) this.dados.curriculo_url = r.url;
    },
    _toBase64(file) {
      return new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result.split(',')[1]);
        r.onerror = rej;
        r.readAsDataURL(file);
      });
    },
    proximo() {
      this.erro = '';
      if (this.passo === 1) {
        if (!this.dados.cpf || !this.dados.nome || !this.dados.cargo_pretendido) {
          this.erro = 'Preencha CPF, nome e cargo.'; return;
        }
        if (!this.roteiro.length) this.carregarRoteiro();
      }
      this.passo++;
    },
    async salvar() {
      this.erro = '';
      if (!this.dados.status) { this.erro = 'Escolha uma decisão.'; return; }
      this.salvando = true;
      const token = localStorage.getItem('rhgng:token');
      const r = await api.salvarEntrevista(token, this.dados);
      this.salvando = false;
      if (!r.ok) { this.erro = r.erro; return; }
      localStorage.removeItem('rhgng:rascunho');
      alert('Entrevista salva: ' + r.id);
      location.hash = '#/painel';
    }
  };
}
</script>
```

- [ ] **Step 2: Atualizar `ui/index.html` rotas**

```html
<template x-if="rota === 'entrevista'"><div><?!= include('ui/entrevista'); ?></div></template>
```

- [ ] **Step 3: Push e teste de ponta a ponta**

Login filial → painel → +Nova → preencher os 4 passos → salvar. Conferir aba da filial na planilha (linha nova) e `_LOG_HISTORICO` (linha de criação).

- [ ] **Step 4: Commit**

```bash
git add ui/entrevista.html ui/index.html
git commit -m "feat: interview wizard 4 steps with auto-save and pdf upload"
```

---

# FASE 4 — Banco de Talentos

### Task 4.1: Tela do Banco de Talentos (filial)

**Files:**
- Create: `ui/banco-talentos.html`
- Modify: `ui/index.html`

- [ ] **Step 1: Implementar**

```html
<div x-data="banco()" x-init="carregar()" class="min-h-screen bg-slate-50">
  <header class="bg-white border-b px-6 py-4 flex justify-between">
    <h1 class="text-lg font-bold">Banco de Talentos — Filial <span x-text="filial"></span></h1>
    <a href="#/painel" class="text-sm text-slate-500 hover:text-slate-900">← Voltar</a>
  </header>

  <main class="max-w-6xl mx-auto p-6">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-4">
      <input x-model="f.cargo" placeholder="Cargo" class="border rounded-lg px-3 py-2">
      <input x-model="f.cidade" placeholder="Cidade" class="border rounded-lg px-3 py-2">
      <input type="number" x-model.number="f.notaMin" placeholder="Nota mín." class="border rounded-lg px-3 py-2">
      <input type="date" x-model="f.dataDe" class="border rounded-lg px-3 py-2">
    </div>

    <div class="bg-white rounded-xl shadow-sm overflow-x-auto">
      <table class="min-w-full text-sm">
        <thead class="bg-slate-100"><tr>
          <th class="px-3 py-2 text-left">Data</th><th class="px-3 py-2 text-left">Nome</th>
          <th class="px-3 py-2 text-left">Cargo</th><th class="px-3 py-2 text-left">Cidade</th>
          <th class="px-3 py-2 text-left">Nota</th><th class="px-3 py-2 text-left">Ação</th>
        </tr></thead>
        <tbody>
          <template x-for="r in filtradas" :key="r.id_entrevista">
            <tr class="border-t hover:bg-slate-50">
              <td class="px-3 py-2" x-text="r.data_hora?.slice(0,10)"></td>
              <td class="px-3 py-2" x-text="r.nome"></td>
              <td class="px-3 py-2" x-text="r.cargo_pretendido"></td>
              <td class="px-3 py-2" x-text="r.cidade"></td>
              <td class="px-3 py-2" x-text="r.nota_geral"></td>
              <td class="px-3 py-2 space-x-1">
                <button @click="agir(r,'Aprovado')" class="text-xs bg-green-600 text-white px-2 py-1 rounded">Aprovar</button>
                <button @click="agir(r,'Reprovado')" class="text-xs bg-red-600 text-white px-2 py-1 rounded">Reprovar</button>
                <button @click="agir(r,'Contratado')" class="text-xs bg-blue-600 text-white px-2 py-1 rounded">Contratado</button>
              </td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
  </main>
</div>

<script>
function banco() {
  return {
    filial: localStorage.getItem('rhgng:filial'),
    registros: [], f: { cargo:'', cidade:'', notaMin:'', dataDe:'' },
    get filtradas() {
      return this.registros
        .filter(r => r.status === 'Banco de Talentos')
        .filter(r => !this.f.cargo || (r.cargo_pretendido || '').toLowerCase().includes(this.f.cargo.toLowerCase()))
        .filter(r => !this.f.cidade || (r.cidade || '').toLowerCase().includes(this.f.cidade.toLowerCase()))
        .filter(r => !this.f.notaMin || Number(r.nota_geral) >= this.f.notaMin)
        .filter(r => !this.f.dataDe || (r.data_hora || '') >= this.f.dataDe);
    },
    async carregar() {
      const token = localStorage.getItem('rhgng:token');
      this.registros = await api.listarFilial(token);
    },
    async agir(r, novo) {
      let motivo = '';
      if (novo === 'Reprovado') {
        motivo = prompt('Motivo da reprovação:') || '';
        if (!motivo) return;
      }
      const token = localStorage.getItem('rhgng:token');
      const resp = await api.atualizarStatus(token, { id: r.id_entrevista, para_status: novo, motivo });
      if (resp.ok) this.carregar(); else alert(resp.erro);
    }
  };
}
</script>
```

- [ ] **Step 2: Atualizar `ui/index.html`**

```html
<template x-if="rota === 'banco'"><div><?!= include('ui/banco-talentos'); ?></div></template>
```

- [ ] **Step 3: Push e teste**

Aprovar um candidato a partir do banco → conferir status atualizado na planilha + linha no `_LOG_HISTORICO`.

- [ ] **Step 4: Commit**

```bash
git add ui/banco-talentos.html ui/index.html
git commit -m "feat: filial talent bank with status actions"
```

---

# FASE 5 — Console Admin

### Task 5.1: Endpoints de relatório

**Files:**
- Modify: `src/Relatorios.gs`

- [ ] **Step 1: Implementar**

```javascript
// src/Relatorios.gs

function listarTodas(token, filtros) {
  Auth.exigir(token, 'admin');
  const all = Repo.listTodas();
  const f = filtros || {};
  return all.filter(r => {
    if (f.cpf      && String(r.cpf).replace(/\D/g,'').indexOf(String(f.cpf).replace(/\D/g,'')) < 0) return false;
    if (f.nome     && !(r.nome || '').toLowerCase().includes(f.nome.toLowerCase())) return false;
    if (f.cargo    && !(r.cargo_pretendido || '').toLowerCase().includes(f.cargo.toLowerCase())) return false;
    if (f.filial   && String(r.filial) !== String(f.filial)) return false;
    if (f.status   && r.status !== f.status) return false;
    if (f.dataDe   && (r.data_hora || '') < f.dataDe) return false;
    if (f.dataAte  && (r.data_hora || '') > f.dataAte + 'T23:59:59') return false;
    return true;
  });
}

function dashboard(token) {
  Auth.exigir(token, 'admin');
  const all = Repo.listTodas();
  const porFilial = {}, porStatus = {}, porMes = {}, porCargo = {};
  let somaNotas = 0, totalNotas = 0;
  all.forEach(r => {
    porFilial[r.filial] = (porFilial[r.filial] || 0) + 1;
    porStatus[r.status] = (porStatus[r.status] || 0) + 1;
    const mes = (r.data_hora || '').slice(0, 7);
    if (mes) porMes[mes] = (porMes[mes] || 0) + 1;
    porCargo[r.cargo_pretendido] = (porCargo[r.cargo_pretendido] || 0) + 1;
    if (typeof r.nota_geral === 'number') { somaNotas += r.nota_geral; totalNotas++; }
  });
  return {
    total: all.length,
    porFilial, porStatus, porMes, porCargo,
    mediaNotaGeral: totalNotas ? Math.round((somaNotas/totalNotas)*100)/100 : 0,
    bancoTalentos: all.filter(r => r.status === 'Banco de Talentos').length,
    aprovados: all.filter(r => r.status === 'Aprovado').length,
    contratados: all.filter(r => r.status === 'Contratado').length,
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/Relatorios.gs
git commit -m "feat: admin reports endpoints"
```

---

### Task 5.2: Tela Admin — Dashboard + busca + exportação

**Files:**
- Create: `ui/admin.html`
- Modify: `ui/index.html`

- [ ] **Step 1: Implementar**

```html
<div x-data="adminPanel()" x-init="carregar()" class="min-h-screen bg-slate-50">
  <header class="bg-white border-b px-6 py-4 flex justify-between">
    <div><h1 class="text-xl font-bold">Console Admin</h1><p class="text-xs text-slate-500">Visão consolidada de todas as filiais</p></div>
    <div class="space-x-3">
      <a href="#/admin-config" class="text-sm text-slate-500 hover:text-slate-900">⚙ Configurações</a>
      <button @click="sair" class="text-sm text-slate-500 hover:text-slate-900">Sair</button>
    </div>
  </header>

  <main class="max-w-7xl mx-auto p-6 space-y-6">
    <!-- Cards -->
    <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
      <template x-for="c in cards">
        <div class="bg-white rounded-xl p-4 shadow-sm">
          <p class="text-xs uppercase text-slate-500" x-text="c.label"></p>
          <p class="text-2xl font-bold" x-text="c.valor"></p>
        </div>
      </template>
    </div>

    <!-- Gráficos -->
    <div class="grid md:grid-cols-2 gap-6">
      <div class="bg-white p-4 rounded-xl shadow-sm"><h3 class="font-semibold mb-2">Por filial</h3><canvas id="chFilial" height="180"></canvas></div>
      <div class="bg-white p-4 rounded-xl shadow-sm"><h3 class="font-semibold mb-2">Por status</h3><canvas id="chStatus" height="180"></canvas></div>
      <div class="bg-white p-4 rounded-xl shadow-sm"><h3 class="font-semibold mb-2">Por mês</h3><canvas id="chMes" height="180"></canvas></div>
      <div class="bg-white p-4 rounded-xl shadow-sm"><h3 class="font-semibold mb-2">Top cargos</h3><canvas id="chCargo" height="180"></canvas></div>
    </div>

    <!-- Busca -->
    <div class="bg-white rounded-xl shadow-sm p-4">
      <h3 class="font-semibold mb-3">Busca global</h3>
      <div class="grid grid-cols-2 md:grid-cols-6 gap-2 mb-3">
        <input x-model="f.cpf" placeholder="CPF" class="border rounded-lg px-3 py-2">
        <input x-model="f.nome" placeholder="Nome" class="border rounded-lg px-3 py-2">
        <input x-model="f.cargo" placeholder="Cargo" class="border rounded-lg px-3 py-2">
        <select x-model="f.filial" class="border rounded-lg px-3 py-2">
          <option value="">Filial</option>
          <template x-for="fi in ctx.filiais"><option :value="fi.codigo" x-text="fi.codigo + ' - ' + fi.nome"></option></template>
        </select>
        <select x-model="f.status" class="border rounded-lg px-3 py-2"><option value="">Status</option>
          <template x-for="s in ctx.opcoes.status"><option x-text="s"></option></template>
        </select>
        <button @click="buscar" class="bg-slate-900 text-white rounded-lg">Buscar</button>
      </div>
      <div class="flex justify-between text-sm mb-2">
        <span x-text="resultado.length + ' resultados'"></span>
        <button @click="exportar" class="text-blue-600 hover:underline">⬇ Exportar CSV</button>
      </div>
      <div class="overflow-x-auto">
        <table class="min-w-full text-sm">
          <thead class="bg-slate-100"><tr>
            <th class="px-3 py-2 text-left">Data</th><th class="px-3 py-2 text-left">Filial</th>
            <th class="px-3 py-2 text-left">Nome</th><th class="px-3 py-2 text-left">CPF</th>
            <th class="px-3 py-2 text-left">Cargo</th><th class="px-3 py-2 text-left">Nota</th>
            <th class="px-3 py-2 text-left">Status</th>
          </tr></thead>
          <tbody>
            <template x-for="r in resultado" :key="r.id_entrevista">
              <tr class="border-t hover:bg-slate-50">
                <td class="px-3 py-2" x-text="r.data_hora?.slice(0,10)"></td>
                <td class="px-3 py-2" x-text="r.filial"></td>
                <td class="px-3 py-2" x-text="r.nome"></td>
                <td class="px-3 py-2" x-text="r.cpf"></td>
                <td class="px-3 py-2" x-text="r.cargo_pretendido"></td>
                <td class="px-3 py-2" x-text="r.nota_geral"></td>
                <td class="px-3 py-2" x-text="r.status"></td>
              </tr>
            </template>
          </tbody>
        </table>
      </div>
    </div>
  </main>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
function adminPanel() {
  return {
    ctx: { filiais:[], opcoes:{} }, cards: [], resultado: [],
    f: { cpf:'', nome:'', cargo:'', filial:'', status:'', dataDe:'', dataAte:'' },
    async carregar() {
      const token = localStorage.getItem('rhgng:token');
      this.ctx = await api.bootstrap(token);
      const d = await api.dashboard(token);
      this.cards = [
        { label: 'Total entrevistas', valor: d.total },
        { label: 'Banco de Talentos', valor: d.bancoTalentos },
        { label: 'Aprovados', valor: d.aprovados },
        { label: 'Contratados', valor: d.contratados },
        { label: 'Média nota geral', valor: d.mediaNotaGeral },
      ];
      this._desenhaGraficos(d);
      this.buscar();
    },
    _desenhaGraficos(d) {
      const mk = (id, labels, data, type='bar') => new Chart(document.getElementById(id), {
        type, data: { labels, datasets: [{ data, backgroundColor: '#1e293b' }] },
        options: { plugins:{ legend:{ display:false } }, scales: type==='bar' ? { y:{ beginAtZero:true } } : {} }
      });
      mk('chFilial', Object.keys(d.porFilial), Object.values(d.porFilial));
      mk('chStatus', Object.keys(d.porStatus), Object.values(d.porStatus), 'doughnut');
      mk('chMes', Object.keys(d.porMes).sort(), Object.keys(d.porMes).sort().map(k => d.porMes[k]), 'line');
      const topCargos = Object.entries(d.porCargo).sort((a,b)=>b[1]-a[1]).slice(0,5);
      mk('chCargo', topCargos.map(x=>x[0]), topCargos.map(x=>x[1]));
    },
    async buscar() {
      const token = localStorage.getItem('rhgng:token');
      this.resultado = await api.listarTodas(token, this.f);
    },
    exportar() {
      if (!this.resultado.length) return;
      const head = Object.keys(this.resultado[0]);
      const csv = [head.join(';')].concat(this.resultado.map(r =>
        head.map(k => '"' + String(r[k] ?? '').replace(/"/g,'""') + '"').join(';'))).join('\n');
      const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = 'rh-gng-export.csv';
      a.click();
    },
    sair() {
      const t = localStorage.getItem('rhgng:token');
      api.authLogout(t).finally(() => { localStorage.clear(); location.hash='#/login'; location.reload(); });
    }
  };
}
</script>
```

- [ ] **Step 2: Atualizar `ui/index.html`**

```html
<template x-if="rota === 'admin'"><div><?!= include('ui/admin'); ?></div></template>
```

- [ ] **Step 3: Push e teste**

Login admin → dashboard com gráficos → busca filtrada → exportar CSV.

- [ ] **Step 4: Commit**

```bash
git add ui/admin.html ui/index.html
git commit -m "feat: admin dashboard with charts, search and csv export"
```

---

### Task 5.3: Tela Admin — Configurações (CRUD)

**Files:**
- Create: `ui/admin-config.html`
- Modify: `src/Config.gs`
- Modify: `ui/index.html`

- [ ] **Step 1: Adicionar endpoints CRUD em `src/Config.gs`**

Append:

```javascript
function salvarCargo(token, payload) {
  Auth.exigir(token, 'admin');
  Repo.upsertCargo(payload.nome_cargo, payload.ativo || 'sim');
  return { ok: true };
}
function salvarRoteiroItem(token, item) {
  Auth.exigir(token, 'admin');
  Repo.upsertRoteiro(item);
  return { ok: true };
}
function excluirRoteiroItem(token, id) {
  Auth.exigir(token, 'admin');
  Repo.deleteRoteiro(id);
  return { ok: true };
}
function salvarCriterio(token, item) {
  Auth.exigir(token, 'admin');
  Repo.upsertCriterio(item);
  return { ok: true };
}
function trocarSenhaFilial(token, codigo, novaSenha) {
  return Auth.trocarSenhaFilial(token, codigo, novaSenha);
}
```

- [ ] **Step 2: Criar `ui/admin-config.html`**

```html
<div x-data="cfg()" x-init="carregar()" class="min-h-screen bg-slate-50">
  <header class="bg-white border-b px-6 py-4 flex justify-between">
    <h1 class="text-xl font-bold">Configurações</h1>
    <a href="#/admin" class="text-sm text-slate-500 hover:text-slate-900">← Voltar</a>
  </header>

  <main class="max-w-5xl mx-auto p-6 space-y-6">
    <!-- Cargos -->
    <section class="bg-white rounded-xl p-4 shadow-sm">
      <h2 class="font-semibold mb-3">Cargos</h2>
      <div class="flex gap-2 mb-3">
        <input x-model="novoCargo" placeholder="Novo cargo" class="border rounded-lg px-3 py-2 flex-1">
        <button @click="addCargo" class="bg-slate-900 text-white px-4 rounded-lg">Adicionar</button>
      </div>
      <ul class="text-sm grid grid-cols-2 md:grid-cols-3 gap-1">
        <template x-for="c in cargos"><li class="border rounded px-2 py-1" x-text="c"></li></template>
      </ul>
    </section>

    <!-- Roteiro -->
    <section class="bg-white rounded-xl p-4 shadow-sm">
      <h2 class="font-semibold mb-3">Roteiro de perguntas</h2>
      <table class="min-w-full text-sm mb-3">
        <thead class="bg-slate-100"><tr><th class="px-2 py-1">ID</th><th>Cargo</th><th>Ordem</th><th>Pergunta</th><th>Tipo</th><th></th></tr></thead>
        <tbody>
          <template x-for="r in roteiro" :key="r.id">
            <tr class="border-t">
              <td class="px-2 py-1" x-text="r.id"></td><td x-text="r.cargo"></td>
              <td x-text="r.ordem"></td><td x-text="r.pergunta"></td><td x-text="r.tipo"></td>
              <td><button @click="excluirRoteiro(r.id)" class="text-red-600 text-xs">excluir</button></td>
            </tr>
          </template>
        </tbody>
      </table>
      <div class="grid grid-cols-5 gap-2">
        <input x-model="nr.id" placeholder="id ex: q5" class="border rounded px-2 py-1">
        <input x-model="nr.cargo" placeholder="cargo ou TODOS" class="border rounded px-2 py-1">
        <input type="number" x-model.number="nr.ordem" placeholder="ordem" class="border rounded px-2 py-1">
        <input x-model="nr.pergunta" placeholder="pergunta" class="border rounded px-2 py-1 col-span-1">
        <select x-model="nr.tipo" class="border rounded px-2 py-1"><option>texto</option><option>sim-nao</option><option>escala</option></select>
      </div>
      <button @click="addRoteiro" class="mt-2 bg-slate-900 text-white px-4 py-2 rounded-lg">Salvar pergunta</button>
    </section>

    <!-- Critérios -->
    <section class="bg-white rounded-xl p-4 shadow-sm">
      <h2 class="font-semibold mb-3">Critérios de avaliação</h2>
      <table class="min-w-full text-sm mb-3">
        <thead class="bg-slate-100"><tr><th class="px-2 py-1">ID</th><th>Nome</th><th>Escala</th><th>Peso</th><th>Ativo</th></tr></thead>
        <tbody>
          <template x-for="c in criterios" :key="c.id">
            <tr class="border-t">
              <td class="px-2 py-1" x-text="c.id"></td><td x-text="c.nome_criterio"></td>
              <td x-text="c.escala_max"></td><td x-text="c.peso"></td><td x-text="c.ativo"></td>
            </tr>
          </template>
        </tbody>
      </table>
      <div class="grid grid-cols-5 gap-2">
        <input x-model="nc.id" placeholder="id ex: c6" class="border rounded px-2 py-1">
        <input x-model="nc.nome_criterio" placeholder="nome" class="border rounded px-2 py-1">
        <input type="number" x-model.number="nc.escala_max" placeholder="escala (ex 5)" class="border rounded px-2 py-1">
        <input type="number" x-model.number="nc.peso" placeholder="peso" class="border rounded px-2 py-1">
        <select x-model="nc.ativo" class="border rounded px-2 py-1"><option>sim</option><option>não</option></select>
      </div>
      <button @click="addCriterio" class="mt-2 bg-slate-900 text-white px-4 py-2 rounded-lg">Salvar critério</button>
    </section>

    <!-- Senhas de filial -->
    <section class="bg-white rounded-xl p-4 shadow-sm">
      <h2 class="font-semibold mb-3">Trocar senha de filial</h2>
      <div class="flex gap-2">
        <select x-model="sf.codigo" class="border rounded px-2 py-1">
          <option value="">Filial</option>
          <template x-for="f in filiais"><option :value="f.codigo" x-text="f.codigo + ' - ' + f.nome"></option></template>
        </select>
        <input x-model="sf.senha" placeholder="Nova senha" class="border rounded px-2 py-1 flex-1">
        <button @click="trocarSenha" class="bg-slate-900 text-white px-4 rounded-lg">Trocar</button>
      </div>
    </section>
  </main>
</div>

<script>
function cfg() {
  return {
    cargos:[], roteiro:[], criterios:[], filiais:[],
    novoCargo:'', nr:{id:'',cargo:'TODOS',ordem:0,pergunta:'',tipo:'texto'},
    nc:{id:'',nome_criterio:'',escala_max:5,peso:1,ativo:'sim'},
    sf:{codigo:'',senha:''},
    async carregar() {
      const t = localStorage.getItem('rhgng:token');
      const b = await api.bootstrap(t);
      this.cargos = b.cargos; this.criterios = b.criterios; this.filiais = b.filiais;
      this.roteiro = await api.getRoteiro(t, 'TODOS');
    },
    async addCargo() {
      const t = localStorage.getItem('rhgng:token');
      await api.salvarCargo(t, { nome_cargo: this.novoCargo, ativo: 'sim' });
      this.novoCargo = ''; this.carregar();
    },
    async addRoteiro() {
      const t = localStorage.getItem('rhgng:token');
      await api.salvarRoteiroItem(t, this.nr);
      this.nr = { id:'', cargo:'TODOS', ordem:0, pergunta:'', tipo:'texto' }; this.carregar();
    },
    async excluirRoteiro(id) {
      const t = localStorage.getItem('rhgng:token');
      await api.excluirRoteiroItem(t, id); this.carregar();
    },
    async addCriterio() {
      const t = localStorage.getItem('rhgng:token');
      await api.salvarCriterio(t, this.nc);
      this.nc = { id:'', nome_criterio:'', escala_max:5, peso:1, ativo:'sim' }; this.carregar();
    },
    async trocarSenha() {
      if (!this.sf.codigo || !this.sf.senha) return;
      const t = localStorage.getItem('rhgng:token');
      const r = await api.trocarSenhaFilial(t, this.sf.codigo, this.sf.senha);
      if (r.ok) { alert('Senha trocada.'); this.sf = { codigo:'', senha:'' }; }
    }
  };
}
</script>
```

- [ ] **Step 3: Atualizar `ui/index.html`**

```html
<template x-if="rota === 'admin-config'"><div><?!= include('ui/admin-config'); ?></div></template>
```

- [ ] **Step 4: Push e teste**

Admin → ⚙ Configurações → adicionar cargo → conferir na planilha. Adicionar pergunta de roteiro → trocar senha de uma filial → logar com a nova.

- [ ] **Step 5: Commit**

```bash
git add ui/admin-config.html ui/index.html src/Config.gs
git commit -m "feat: admin config screen for cargos roteiro criterios senhas"
```

---

# FASE 6 — Polimento, testes, docs

### Task 6.1: Smoke test completo manual

**Files:**
- Create: `docs/SMOKE-TEST.md`

- [ ] **Step 1: Documentar roteiro**

```markdown
# Smoke Test — RH G&G

## Pré
- setupPlanilha() executado, senhas anotadas.
- Web app implantado, URL acessível.

## Fluxo
1. Login filial 464 com senha → cards zerados.
2. + Nova entrevista → CPF válido inédito → passo 2 → 3 → 4 → "Banco de Talentos" → salvar.
3. Conferir aba `464-MT-TREVO` (linha nova) e `_LOG_HISTORICO` (linha de criação).
4. Voltar ao painel → registro aparece na tabela.
5. Banco de Talentos → registro listado → Aprovar → status atualiza + nova linha no log.
6. Logout. Login admin → dashboard mostra contagens corretas → busca pelo CPF retorna o registro → exportar CSV → conferir arquivo.
7. Configurações → adicionar cargo "Vendedor PJ" → voltar ao login filial → nova entrevista mostra o cargo no dropdown.
8. Tentar nova entrevista com mesmo CPF → alerta amarelo de histórico aparece.

## Critério de sucesso
Todos os passos sem erro; registros consistentes entre Sheets, logs e UI.
```

- [ ] **Step 2: Executar o smoke test e marcar tudo**

- [ ] **Step 3: Commit**

```bash
git add docs/SMOKE-TEST.md
git commit -m "docs: smoke test plan"
```

---

### Task 6.2: Documentação final

**Files:**
- Create: `docs/CHANGELOG.md`
- Create: `docs/LGPD.md`
- Create: `docs/MIGRACAO.md`
- Create: `README.md`

- [ ] **Step 1: `README.md`**

```markdown
# RH G&G

Web app de Gente & Gestão para conduzir entrevistas e gerir banco de talentos.

## Setup
1. `npm install -g @google/clasp && clasp login`
2. `clasp push`
3. No editor: rodar `setupPlanilha()` (anote senhas geradas no Logger).
4. Implantar como Web App ("Executar como: usuário acessando").

## Uso
- Filial: entrar com a senha gerada.
- Admin: usuário `admin`, senha gerada.

Spec: `docs/superpowers/specs/2026-06-01-rh-gng-design.md`
```

- [ ] **Step 2: `docs/CHANGELOG.md`**

```markdown
# Changelog
## 0.1.0 — 2026-06-01
- MVP funcional: login, entrevista, banco, admin.
```

- [ ] **Step 3: `docs/LGPD.md`**

```markdown
# LGPD — RH G&G

**Controlador:** Grupo G&G
**Finalidade:** seleção e contratação de pessoal.
**Base legal:** consentimento + execução de procedimentos preliminares ao contrato.
**Dados tratados:** identificação, contato, qualificação profissional, avaliações de entrevista.
**Retenção:** até 2 anos após a entrevista (após isso, arquivar/anonimizar).
**Direitos do titular:** acesso, correção, exclusão — solicitar ao RH central.
**Compartilhamento:** apenas entre filiais e RH central, conforme finalidade.
```

- [ ] **Step 4: `docs/MIGRACAO.md`**

```markdown
# Migração futura para Vercel + Supabase

## Estratégia
- Repo.gs → substituir por `SupabaseRepo` (mesma API).
- Endpoints `.gs` → portar para Next.js API routes / Edge Functions.
- UI Alpine → reescrever em Next.js (mesmo JSON de dados).
- Sheets → migração ETL única; depois, planilha apenas como visualização.

## Ordem sugerida
1. Modelar tabelas Supabase espelhando colunas das abas.
2. Migrar dados via script `migrate.ts`.
3. Subir API Next.js em paralelo, redirecionar gradualmente.
4. Desativar Apps Script.
```

- [ ] **Step 5: Commit**

```bash
git add README.md docs/
git commit -m "docs: changelog, lgpd, migration plan, readme"
```

---

### Task 6.3: Deploy final

- [ ] **Step 1: Push final**

```bash
clasp push
```

- [ ] **Step 2: Criar implantação versionada**

No editor Apps Script: **Implantar > Nova implantação > Web app**.
- Descrição: `v0.1.0 MVP`
- Executar como: `O usuário que acessa`
- Quem tem acesso: `Qualquer pessoa`

Copiar URL.

- [ ] **Step 3: Compartilhar URL com filiais + entregar senhas geradas**

- [ ] **Step 4: Tag git**

```bash
git tag -a v0.1.0 -m "MVP funcional"
```

---

## Self-Review (já realizado)

**Cobertura do spec:** todas as seções do design (arquitetura, planilha completa com colunas A–AR, telas 4.1–4.6, segurança, testes, deploy, riscos, migração futura) têm tarefas correspondentes nas Fases 0–6.

**Placeholders:** zero — cada step tem código completo, comando exato ou conteúdo do arquivo.

**Consistência de tipos:** nomes de campos das tarefas batem com cabeçalhos definidos em `setupPlanilha` (COLUNAS_FILIAL) e com payload do `salvarEntrevista`. Função `nomeAbaFilial` é definida em `Setup.gs` e usada em `Repo.gs`. Endpoints `api.<nome>` no frontend batem com funções globais nos `.gs` (authLogin, authLogout, bootstrap, getRoteiro, buscarPorCPF, salvarEntrevista, atualizarStatus, listarFilial, listarTodas, dashboard, salvarCargo, salvarRoteiroItem, excluirRoteiroItem, salvarCriterio, trocarSenhaFilial, uploadPDF).
