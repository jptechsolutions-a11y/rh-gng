// Stub for the `server-only` package under Vitest (Node env has no Next.js
// module resolution). The real package only throws when imported from a
// Client Component bundle, which is irrelevant in unit tests.
export {};
